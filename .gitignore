# Blacklist the highest level directory with /*
/*

# Whitelist the files that should be part of the respository.
!.github
!.run
# Note:
# Do not use single or double asteriks at the end. They are not working properly.
# To ignore all files in a dir recursively just add / at the end.
!src/
src/main/resources/static/client/
!.editorconfig
!.gitattributes
!.gitignore
!.mvn
!create-promptchain-db-user.*
!dev-env.properties
!docker-compose.yml
!docker-compose-postgres.yml
!docker-compose-redis.yml
!Dockerfile
!GIT-GITHUB-GPG-SETUP.md
!GIT_SETUP.md
!LICENSE
!pom.xml
!prune-all-images.*
!README.md
!start-postgres-container.*
!start-redis-container.*
!stop-postgres-container.*
!stop-redis-container.*

#TODO-Jawad: This will not be needed because build output will be sent to another dir when npm build is configured in mvn
# ------------------------------
# Ignore only Next.js build artifacts inside static/client
# ------------------------------
src/main/resources/static/client/node_modules/
src/main/resources/static/client/.next/
src/main/resources/static/client/out/
src/main/resources/static/client/.turbo/
src/main/resources/static/client/.env*
src/main/resources/static/client/.DS_Store
